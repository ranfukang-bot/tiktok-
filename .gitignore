node_modules/
config/settings.json
config/accounts.json
state/*.json
!state/.gitkeep
*.log

# 诊断快照：文件名里就是真实账号名，内容含商品ID和视频文件名。
# 仓库是公开的，这些绝对不能提交。脚本本身(diagnostics/*.mjs)是可以提交的。
diagnostics/output/

# 人工确认测试用的隔离夹具(建在仓库内是为了复用 node_modules)，崩溃时可能残留
.confirmation-test-*/
